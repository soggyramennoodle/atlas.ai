// Vitest stub for the `server-only` package.
// The real package throws unless the `react-server` export condition is active
// (Next.js RSC builds). This stub is a no-op so pure helpers can be unit-tested.
export {};
